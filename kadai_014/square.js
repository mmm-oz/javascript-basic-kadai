// 引数の2乗を戻り値とする
const calc = (double) => {
    return double*double;
}

// コンソールに表示
console.log(calc(10));

// 上記の戻り値を引数だけで出せないか

// const calc2 = (double2) => {
//     console.log(double2*double2);
// }

// calc2(10);